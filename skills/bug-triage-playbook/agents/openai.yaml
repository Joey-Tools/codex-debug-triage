interface:
  display_name: "Bug Triage Playbook"
  short_description: "Optional explicit hypothesis-structuring reference"
  default_prompt: "Explicitly use $bug-triage-playbook to structure hypotheses for this local debugging task."
policy:
  allow_implicit_invocation: false
